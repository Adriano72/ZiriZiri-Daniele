<Alloy>
    <Window class="container" navBarHidden="true" onOpen="showIndicator">

        <!-- Define the styling properties in the TSS file -->
        <ActivityIndicator id="spinner" message="  Loading..."/>
    </Window>
</Alloy>