var args = arguments[0] || {};

//Ti.API.info("VALORE PASSATO: "+args.importo);

$.row.id_code = args.id_code,
$.description.text = '  '+icons.link;
$.titolo.text = args.titolo;
$.descrizione.text = args.descrizione,
$.content.text = args.content;